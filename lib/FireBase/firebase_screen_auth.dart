import 'package:flutter/material.dart';

class FirebaseScreenAuth extends StatelessWidget {
  static String routeName = "/authscreen";

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
